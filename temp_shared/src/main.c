#include <stdio.h>
#include "hello.h" // hello.h is a header file that contains the function prototype for hello() 

int main(){
    HelloWorld(); // Call the function from helloWorld.c
    HelloCuong(); // Call the function from helloCuong.c
    return 0;
}